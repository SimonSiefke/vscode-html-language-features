{
  "name": "typescript-composite-lerna",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "tsc -b --watch",
    "build": "tsc -b",
    "postinstall": "lerna bootstrap && tsc -b",
    "package": "npm run build && node scripts/package.js && cd dist && vsce package"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.10",
    "fs-extra": "^8.1.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2"
  }
}
